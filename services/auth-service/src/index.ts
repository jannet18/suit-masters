// import { buildApp } from "./app";
// import { PORT } from "./utils/kinde";

// const app = buildApp();

// app.listen({ port: PORT, host: "0.0.0.0" }).catch((err) => {
//   app.log.error(err);
//   process.exit(1);
// });
